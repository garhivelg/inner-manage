extends ../base.pug

block body
  | {% import "bootstrap/wtf.html" as wtf %}
  .content-section
    .outer
      .middle
        .inner
          .center
            h1 Assign Departments and Roles
            br
            p
              | Select a department and role to assign to
              span(style="color: #aec251;") {{ employee.first_name }} {{ employee.last_name }}
            br
            | {{ wtf.quick_form(form) }}
